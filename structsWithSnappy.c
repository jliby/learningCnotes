#include <stdio.h>

void catalog(struct fish f) {
    printf("%s is a %s with %i teeth. He is %i\n",
    fish.name, fish.species, fist.teeth, fish.age)
}

int main(){
    struct fish snappy = {"Snappy", "Piranha", 69, 4};
    catalog(snappy);
    return 0;
}